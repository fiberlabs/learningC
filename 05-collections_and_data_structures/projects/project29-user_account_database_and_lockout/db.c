#include <stdio.h>
#include <string.h>

#define CAPACITY 3

struct User
{
    int id;
    int pin;
    int locked; //bool
    int failures; //tally
};


int main()
{
    struct User users[CAPACITY];
    int count = 0;

    //manually creating users
    

    return 0;
}