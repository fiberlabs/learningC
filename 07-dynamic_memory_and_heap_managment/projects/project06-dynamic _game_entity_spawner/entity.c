#include <stdio.h>

struct Enemy {
    char *name;
    int cordinate_x;
    int cordinate_y;
};

struct Enemy *spawn_enemy(char *name_parameter, int x_parameter, int y_parameter);

int main() {
    
    return 0;
}